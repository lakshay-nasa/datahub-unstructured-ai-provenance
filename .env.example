# ==========================================
#  DataHub & Governance Configuration
# ==========================================
# Instructions:
# 1. Copy this file and rename it to '.env'
# 2. Update the values below with your local environment details.

# ------------------------------------------
# DataHub Service (GMS)
# ------------------------------------------
# The URL where your local DataHub backend is running.
# Default for Docker Quickstart is http://localhost:8080
DATAHUB_API_URL=http://localhost:8080

# Personal Access Token (PAT) for DataHub.
# Required if Metadata Service Authentication is enabled.
# If using the default quickstart with no auth, leave this blank.
DATAHUB_ACCESS_TOKEN=your_datahub_token_here